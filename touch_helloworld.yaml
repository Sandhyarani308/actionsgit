name:Hello World
on:
  push:
    branches:
      -main
  pull_request:
    branches:
      -main
  workflow_dispatch
jobs:
  hello:
    runs_on:ubuntu_latest
    steps:
      -uses:actions/checkout@v2
      -name:hello world
      run:echo "hello world"
      shell:bash
  goodbye:
    runs_on:ubuntu_latest
    steps:
      
      -name:goodbye helloworld
      run:echo "goodbye helloworld"
      shell:bash
